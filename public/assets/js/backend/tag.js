define(['jquery', 'bootstrap', 'backend', 'table', 'form'], function ($, undefined, Backend, Table, Form) {

    var Controller = {
        index: function () {
            // 初始化表格参数配置
            Table.api.init({
                extend: {
                    index_url: 'tag/index',
                    add_url: 'tag/add',
                    edit_url: 'tag/edit',
                    del_url: 'tag/del',
                    multi_url: 'tag/multi',
                    table: 'lover_tag',
                }
            });

            var table = $("#table");

            // 初始化表格
            table.bootstrapTable({
                url: $.fn.bootstrapTable.defaults.extend.index_url,
                pk: 'id',
                sortName: 'id',
                columns: [
                    [
                        {checkbox: true},
                        {field: 'id', title: __('Id')},
                        {field: 'name', title: __('Name')},
                        {field: 'type', title: __('Type'), searchList: {"personality":__('Type personality'),"hobby":__('Type hobby'),"exercise":__('Type exercise'),"food":__('Type food'),"time":__('Type time'),"skill":__('Type skill'),"other":__('Type other')}, formatter: Table.api.formatter.normal},
                        {field: 'nums', title: __('Nums')},
                        {field: 'operate', title: __('Operate'), table: table, events: Table.api.events.operate, formatter: Table.api.formatter.operate}
                    ]
                ]
            });

            // 为表格绑定事件
            Table.api.bindevent(table);
        },
        add: function () {
            Controller.api.bindevent();
        },
        edit: function () {
            Controller.api.bindevent();
        },
        api: {
            bindevent: function () {
                Form.api.bindevent($("form[role=form]"));
            }
        }
    };
    return Controller;
});